

const Cart = ({ items }) => {


     return (
          <>


               <div
                    className='w-1/4'
                    onClick={() => toggleCart(false)}
               />
          </>
     )
}

export default Cart